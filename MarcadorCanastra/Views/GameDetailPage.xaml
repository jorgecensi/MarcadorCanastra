<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:MarcadorCanastra.Models"
             mc:Ignorable="d"
             x:Class="MarcadorCanastra.Views.GameDetailPage"
             Title="{Binding Title}"
             Visual="Material">
    
    <StackLayout Spacing="20" Padding="15">
        
        <Grid VerticalOptions="FillAndExpand" > 
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Label Text="Round"
                        Grid.Column="0"
                       FontSize="Title"/>
                <Label Text="{Binding Game.Player1.Name}"
                       d:Text="Jogador 1"
                       FontSize="Title"
                       Grid.Column="1"/>
                <Label Text="{Binding Game.Player2.Name}"
                       d:Text="Jogador 2"
                       FontSize="Title"
                        Grid.Column="2"/>
            </Grid>
            
            <CollectionView
               
                Grid.Row="1"
                ItemsSource="{Binding Rounds}
                ">
                
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                                <Label Text="{Binding RoundNumber}"
                                       FontSize="Title"
                                       Grid.Column="0"/>
                                <Label Text="{Binding Player1Score.Total}"
                                       FontSize="Title"
                                       Grid.Column="1"/>
                                <Label Text="{Binding Player2Score.Total}"
                                       FontSize="Title"
                                       Grid.Column="2"/>
                            </Grid>
                        
                    </DataTemplate>
                </CollectionView.ItemTemplate>

            </CollectionView>
                
            <Grid Grid.Row="2"
                  
                  >
                <Label Text="Total"
                        Grid.Column="0"
                       FontSize="Title"
                       VerticalTextAlignment="End"/>
                <Label Text="{Binding Game.FinalScorePlayer1}"
                       d:Text="300"
                        Grid.Column="1"
                       FontAttributes="Bold"
                       FontSize="Title"/>
                <Label Text="{Binding Game.FinalScorePlayer2}"
                       d:Text="400"
                        Grid.Column="2"
                       FontAttributes="Bold"
                       FontSize="Title"/>
            </Grid>
        </Grid>
        <Button Text="Marcar pontos"
                Clicked="AddPlayer1Points_Clicked"
                BackgroundColor="LimeGreen"/>
    </StackLayout>
</ContentPage>